spring:
  config:
    activate:
      on-profile: "dev"
  datasource:
    url: jdbc:mysql://localhost:3306/unics?createDatabaseIfNotExist=true
    username: ${DATABASE_USERNAME:root}
    password: ${DATABASE_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        show_sql: true
  mail:
    host: ${EMAIL_HOST:smtp.gmail.com}
    username: ${EMAIL_SENDER:dummy@example.com}
    password: ${EMAIL_PASSWORD:dummypassword}
    properties:
      mail:
        transport.protocol: smtp
        smtp.auth: true
        starttls.enable: true
        ssl.enable: false

logging:
  file:
    name: log/unics-dev.log
  level:
    org:
      springframework:
        security: DEBUG

server:
  servlet:
    session:
      timeout: 5m
      cookie:
        http-only: true
#        secure: true # Only if using HTTPS
  port: 6050
  error:
    include-message: always

email:
  active: true

mock-data: false

secret-string: "HGJKMVNBSCHGDHYFIEKHGNVFHBKMDNHYEIKANJNURBHGEBNUYABDIURBNAUEYBATVBNOWURFBYVAAKMKJNDCZKDC"